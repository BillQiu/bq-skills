# 仓库约定

- 始终使用中文回应
- 所有 skill 都放在 `skills/` 目录下
- 当前不对 skill 做类型分类
- 每个 skill 使用独立目录，例如 `skills/<skill-name>/`
- 每个 skill 至少包含 `SKILL.md`
- `references/` 和 `assets/` 按需创建，不强制存在
- 初始化仓库时不创建示例 skill、模板目录、CI 或工作流文件
